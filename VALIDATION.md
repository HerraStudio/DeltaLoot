# DeltaLoot 1.2.3 验证记录

验证日期：2026-09-18。

- 构建环境：Microsoft OpenJDK 21.0.12.1、Apache Maven 3.9.11，目标 Java 21。
- 编译依赖：Paper API `1.21.11-R0.1-SNAPSHOT`，本次解析版本为 `1.21.11-R0.1-20260511.115010-91`。
- 执行 `mvn verify`（生产代码与测试均重新编译）：BUILD SUCCESS。
- 自动化测试：70 项通过，0 失败、0 错误、0 跳过。

| 测试组 | 数量 | 验证范围 |
| --- | ---: | --- |
| PluginSettingsTest | 11 | YAML 解析、配置类型/边界、无效输入拒绝、六行全部 54 格容量 |
| InventoryTransferTest | 8 | 合并堆叠、空间不足、元数据区分、不可堆叠物品、输入不变性 |
| SearchProgressTest | 11 | 搜索开始、完成、取消、重启、防抢占、计时溢出 |
| LootTableTest | 9 | 格数、空格、随机位置、重复抽取、种子复现 |
| WeightedPickerTest | 8 | 权重区间、概率分布、非法权重及精度边界 |
| LootInterfaceTest | 16 | 开箱只扫描有物品的格子、空格直接隐藏并跳过、领取后留空、跳过已揭示格、关闭重开接续、受伤自动恢复与再次受伤延迟、到期刷新及使用中不刷新、旧存档扩容保存、空格无占位物、无顶部进度条或动作栏、搜索图标无百分比和说明且仍按时揭示 |
| CraftEngineSearchMaskTest | 7 | 新旧物品构造接口适配、完整堆栈克隆、缺失物品降级、日志去重、重载后重新尝试、异常返回值拒绝 |

CraftEngine 适配测试使用按已核对公开接口构造的测试对象与模拟 ItemStack，未运行真实 CraftEngine。实际物品注册、模型及客户端资源包显示需要在用户服务器验证。

配置与背包测试使用 Mockito 模拟依赖服务端注册表的材质/物品元数据；GUI 回归测试执行真实管理器及会话逻辑，并模拟玩家、世界、库存与图标元数据。测试实际写入及重新读取空格状态 YAML，验证扩容与揭示状态持久化；没有启动真实 Paper 服务端。尚未完成真实客户端视觉效果、多人网络延迟、真实物品序列化存档和其他插件组合的联机验证。上服验收步骤见 README。

交付 JAR 仅包含插件代码、配置与描述文件；不包含 Paper API 或测试依赖。源码包包含可重新构建的 Maven 工程与全部测试。

